{
  "name": "react-workshop",
  "version": "0.0.1",
  "description": "React Workshop",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-mermaid": "cp node_modules/mermaid/dist/{mermaid.min.js,mermaid.css} template",
    "start": "bs serve slides --skip-open --port 4100",
    "build": "bs export slides/*.md --no-inline --output dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/roberto/react-workshop.git"
  },
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/roberto/react-workshop/issues"
  },
  "homepage": "https://github.com/roberto/react-workshop#readme",
  "dependencies": {
    "backslide": "^1.2.1",
    "gh-pages": "^1.0.0",
    "mermaid": "^7.0.3"
  }
}
